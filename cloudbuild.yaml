steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/sinhala-transcriber'
      - '.'

images:
  - 'gcr.io/$PROJECT_ID/sinhala-transcriber'

options:
  logging: CLOUD_LOGGING_ONLY
